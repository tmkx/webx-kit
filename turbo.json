{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist"]
    },
    "lint:type": {
      "dependsOn": ["^lint:type"],
      "outputs": []
    },
    "pretest": {
      "outputs": ["dist"]
    },
    "test": {
      "dependsOn": ["pretest", "^test"]
    }
  }
}
